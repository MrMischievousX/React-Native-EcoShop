import React from "react"
import { Dimensions } from "react-native"

const iconWidth: number = 28
const navIconWidth: number = 24
const profileIcon: number = 66


const { height, width } = Dimensions.get('screen');

export { iconWidth, width, profileIcon, height, navIconWidth }